[project]
name = "httpxrequest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "RogerPierre", email = "rogerpierrereis@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "typing>=3.10.0.0",
]

[project.scripts]
httpxrequest = "httpxrequest:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
